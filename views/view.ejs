<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Hero App | New</title>

    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>

    <style>

        body { 
            padding-top:15px; 
            background-color: #222222;
        }

        img {
            width: 100% !important;
        }
        
        .post_view{
            padding: 1em;
            margin: 1em;
            text-align: center;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="panel panel-danger">
            <div class="text-center text-white bg-dark">
                <h2 class="text-center"><%= poke.name %></h2>
            </div>
            <a href="/archive" class="btn btn-block">BACK TO ARCHIVE</a>

            <div class="panel-body" id="all">

                <div class="row">
                    <div class="post_view">
                        <img src="<%= poke.image %>">
                    </div>
                    <div class="post_view">
                            <%-poke.description %>
                        </div>
                    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
                        <br>
                <% if(logedIn == true){%>

                        <a href="/update/<%= poke.id %>" class="btn btn-block btn-primary">Update</a>
                        <br>
                        <a href="/delete/<%= poke.id %>" class="btn btn-block btn-danger">Delete</a>
                        <%}else{%>
                            <p class="logged_out">Please <a href="/sign_in">Log In</a> before attempting to create a post!</p>
                          <%} %>
                          
                    </div>
                </div>  

            </div>
            <div class="panel-footer text-center">
                <p>All rights reserved 2018 | ShantonWaldien.</p>
            </div>
        </div>
    </div>

</body>
</html>